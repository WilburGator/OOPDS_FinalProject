#include <iostream>
#include "ActivationFunctions.hpp" 
#include <cstdlib>

int main() {
    /*
    Activation functions test cases:
    
    double inputs[] = { -2.0, -1.0, 0.0, 1.0, 2.0 };

    for (double input : inputs) {
        std::cout << "Input: " << input << std::endl;
        std::cout << "Sigmoid(" << input << ") = " << activate(input, SIGMOID) << std::endl;
        std::cout << "ReLU(" << input << ") = " << activate(input, RELU) << std::endl;
        std::cout << "Tanh(" << input << ") = " << activate(input, TANH) << std::endl;
        std::cout << "Step(" << input << ") = " << activate(input, STEP) << std::endl;
        std::cout << std::endl;
    }
    return 0;
    */
}